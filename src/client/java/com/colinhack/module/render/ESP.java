package com.colinhack.module.render;

import com.colinhack.module.Module;

public class ESP extends Module {
    public ESP() {
        super("ESP", "Highlights entities through walls", Category.RENDER);
    }
    // Implementation would require Mixins to rendering
}
